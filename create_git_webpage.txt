

1.


1.1 for Mac won't allow you to gem install jekyll bundler so you do this:

brew install chruby ruby-install
ruby-install ruby 3.4.1

$ cd
$ echo 'source "$(brew --prefix)/opt/chruby/share/chruby/chruby.sh"' >> ~/.zshrc
$ echo 'source "$(brew --prefix)/opt/chruby/share/chruby/auto.sh"' >> ~/.zshrc
$ echo 'chruby ruby-3.4.1' >> ~/.zshrc
$ source ~/.zshrc

$ ruby -v
ruby 3.4.1 (2024-12-25 revision 48d4efcb85) +PRISM [arm64-darwin25]

$ which ruby
/Users/LEILA.BAGHDADBRAHIM/.rubies/ruby-3.4.1/bin/ruby

gem install jekyll bundler

2.
mkdir wiki-demo
cd wiki-demo
bundle init

Edit the generated Gemfile:

ruby
source "https://rubygems.org"

gem "jekyll"
gem "kramdown"


3.
bundle install


4.
http://localhost:4000